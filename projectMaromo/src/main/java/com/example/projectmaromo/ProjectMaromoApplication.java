package com.example.projectmaromo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class for the Project Maromo application.
 *
 * <p>This class is the entry point for the Spring Boot application.</p>
 *
 * @version 1.0
 * @since 2024-06-13
 */
@SpringBootApplication
public class ProjectMaromoApplication {

    /**
     * Main method to run the Spring Boot application.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(ProjectMaromoApplication.class, args);
    }
}
